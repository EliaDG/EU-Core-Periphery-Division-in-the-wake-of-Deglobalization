
# Necessary packages:
install.packages("factoextra")

library(pacman)

p_load("readxl",
       "dplyr",
       "tidyverse",
       "data.table",
       "corrplot",
       "cluster",
       "dendextend",
       "mice",
       "caret",
       "stats",
       "factoextra",
       "VIM"
       )
